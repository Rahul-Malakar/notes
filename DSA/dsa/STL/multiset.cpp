#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main()
{

    ios_base::sync_with_stdio(false);

    //same as set but multiple values can be inserted
    //sorted order
    //find operation returns iterator of first occurence
    
    //erase function if iterator is passed then only that index is deleted 
    //but if value is passed then all the similar elements will be deleted

    return 0;
}