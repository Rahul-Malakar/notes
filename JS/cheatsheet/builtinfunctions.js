// myArray.push(4); // Adds 4 to the end of the array
// myArray.splice(2, 2); // Removes 2 elements starting from index 2
// myArray.sort((a, b) => a - b); // Sorts in ascending order
// myArray.sort((a, b) => b - a); // Sorts in descending order
// reverse function

// what are regular expressions that we write inside join, split and other function 
// substring
// replace
// split and join
// trim

